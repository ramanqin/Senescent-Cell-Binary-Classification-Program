# 第一次运行

这页只讲第一次怎样把流程跑通。各参数的含义和完整文件分工仍看对应子目录的 README。以下命令以 Windows PowerShell 为例，均从项目根目录 `D:\Raman Spectroscopy Aging Analysis` 执行；把路径换成自己电脑上的位置即可。

## 先选任务

| 想做什么 | 使用哪个入口 | 新数据会不会参与训练 |
|---|---|---|
| 从原始光谱得到统一的 1201 点数据 | `data analysis` 中的清洗和光谱批处理 | 不涉及分类模型 |
| 用一批有年轻/衰老标签的数据重新建模并做普通 5 折 | `年轻衰老分析与降维/01_PCA-SVM分类/代码/run_classification.py` | 会；输出新的最终模型 |
| 用已经训练好的 P2/P9 模型直接预测另一批数据 | `P2P9固定预测模型/预测新数据.py` | 不会；这是外部预测 |
| 看逐波数差异、候选位点、小提琴图 | `Significance Analysis and Violin Plot Drawing` | 不训练分类模型 |

`data analysis/PCA_SVM_GUI` 是单独的图形版 PCA-SVM 分析入口，不是清洗和批处理之间的必经步骤。第一次运行建议先用上表的命令行分类入口，避免同时面对两套界面。

## 1. 准备 Python

建议 Python 3.11 或更高版本；此项目已在 Python 3.13 环境中试跑。根目录 `requirements.txt` 是整套流程的统一安装入口，并固定了加载 P2/P9 模型时使用的 scikit-learn 1.9.0；各子目录的依赖文件供单独取用该模块时使用。不要复制或激活 `data analysis/raman_spectrum_filter-main/.venv`，那是原电脑留下的环境。

```powershell
cd 'D:\Raman Spectroscopy Aging Analysis'
python -m venv .venv
$py = (Resolve-Path '.\.venv\Scripts\python.exe').Path
& $py -m pip install -r '.\requirements.txt'
```

若电脑上 `python` 命令不可用，先安装 Python，并在上述第一行换成可用的 `py -3 -m venv .venv`。后面一直用 `$py`，不需要设置 PowerShell 的脚本执行权限。新建的根目录 `.venv` 已加入 `.gitignore`。

## 2. 用随项目附带的数据做一次命令行检查

`示例数据`是人为构造的，年轻/衰老各 5 个编号、每编号 2 条谱。`已预处理光谱`已经使用本项目的默认批处理参数生成，可直接检验下游程序；它不是科研数据。

```powershell
& $py '.\年轻衰老分析与降维\01_PCA-SVM分类\代码\run_classification.py' `
  --input '.\示例数据\已预处理光谱' --output '.\演示输出\分类' `
  --models svm logistic random_forest
```

跑完先找 `演示输出/分类/model_summary.csv`，再看三个模型子目录的 `metrics.json`、`predictions.csv` 和混淆矩阵。这里每组恰好 5 个编号，只是为了让 5 折流程能运行；得到的高分或低分都不能用于研究结论。

差异分析的快速检查用较严格的阈值，避免演示信号刻意构造的差异生成大量图页：

```powershell
& $py '.\Significance Analysis and Violin Plot Drawing\Significance Analysis and Violin Plot Drawing\run_analysis.py' `
  --input '.\示例数据\已预处理光谱' --output-root '.\演示输出\差异分析' `
  --raw-p-threshold 0.001
```

找到新建的 `analysis_run_*`，确认有 `结果说明.txt` 和 `结果图.png`，且没有 `运行失败.txt`。这个小样本在 0.001 阈值下通常没有候选位点；这是预期的，不代表程序失败。真实分析再按研究方案决定筛选阈值。

## 3. 再试一遍原始光谱到预处理的图形流程

要练习上游两步，使用 `示例数据/原始光谱`。两步都不要把输出目录放进输入目录内。

从项目根目录依次启动两个界面；每次关闭窗口后命令才会返回：

```powershell
New-Item -ItemType Directory -Force '.\演示输出\清洗', '.\演示输出\预处理' | Out-Null
Push-Location '.\data analysis\raman_spectrum_filter-main'
& $py '__main__.py'
Pop-Location
Push-Location '.\data analysis\Spectral Batch Processing'
& $py 'app.py'
Pop-Location
```

1. 清洗界面输入选 `示例数据/原始光谱`，输出选项目下的 `演示输出/清洗`。默认参数运行后，应在 `cleaning_run_*/run_manifest.csv` 看见 20 条 `pass`，下一步只用同一次结果的 `passed/`。
2. 批处理界面输入选上一步的 `passed/`，输出选 `演示输出/预处理`。保持“递归读取子文件夹”和“保留输入目录结构”开启，输出格式选 TXT，载入该程序的 `默认参数.json` 并确认 600–1800 cm⁻¹、步长 1。运行后应在 `preprocessing_run_*/` 下看到年轻/衰老各 5 个编号、共 20 条谱，并检查 `run_manifest.csv` 是否全部成功。

预处理文件默认有一行 `Raman_shift_cm-1  Intensity` 表头。分类、固定模型预测和差异分析入口均可读取这种格式；真正的数值数据仍是 1201 行。上面第 2 步为了省去界面操作，直接用了附带的预处理结果。

## 4. 换成自己的数据

原始目录按 `年轻/个体编号/多条光谱.txt`、`衰老/个体编号/多条光谱.txt` 整理。同一个体的重复谱必须留在同一编号下；不要把重复谱当作独立个体。清洗后把 `cleaning_run_*/passed` 交给批处理，后续入口使用整个 `preprocessing_run_*` 目录，而不是原谱或 `passed` 目录。

- 若目标是**重新建模**，每组至少 5 个独立编号，用上面的分类入口；PCA、标准化等都在各训练折内拟合，结果是普通分层 5 折折外评价。
- 若目标是**固定 P2/P9 模型外测**，不要再运行分类训练脚本。改用 `& $py '.\P2P9固定预测模型\预测新数据.py' --input '你的预处理目录' --output '新的预测结果目录'`。输入可以带组别标签供事后评价，也可以只按编号放谱、不告诉程序标签。P2/P9 训练数据的采集设置与标签重合，外测结果必须连同这一限制一起报告。
- 若目标是**差异分析**，两组各至少 2 个独立编号，使用上面的差异分析入口，并在结果里区分原始 p 值与 FDR 校正后的 q 值。

每次先检查清洗和预处理的 `run_manifest.csv`、`run_parameters.json`，再看分类或统计结果。某个编号若没有合格谱，先处理缺失和剔除原因，不要只根据最终准确率判断流程是否正常。输出目录建议每次换新名称，避免混入旧结果。

## 遇到问题先查哪里

| 现象 | 首先检查 |
|---|---|
| 没找到数据或某一组为空 | 输入目录是否选到了 `preprocessing_run_*` 本身；是否保留 `年轻/编号`、`衰老/编号` 层级 |
| 波数轴/1201 点报错 | 是否用的是已预处理光谱；范围是否为 600–1800、步长是否为 1；有没有混入原谱或其他批次设置 |
| 普通 5 折无法启动 | 每组是否至少 5 个独立编号，而非只有 5 条重复谱 |
| 差异分析没有候选位点 | 看阈值、原始 p 和 q 值；没有候选不等于运行失败 |
| 固定模型在新批次大量偏向单一类别 | 同时报告两组识别率，并核对采集设置、培养批次和预处理；不要用外测集临时重训或调阈值后仍称其为独立外测 |

更详细的参数、文件用途和输出解释，回到[项目总 README](README.md)及对应模块 README。
